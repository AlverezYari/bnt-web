# Ignore all files in this directory, as its contents are generated by the `infra` module which doesn't have repo safe tf state, will manually backup the files here
# infra/

# Terraform state, modules and other generated files
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.terraform/
*.tfvars
.terraform*

# Local builds
temp/

# js shit
*.node_modules/
*.npm/
*.yarn/

